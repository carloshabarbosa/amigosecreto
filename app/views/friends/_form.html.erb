
<% if @friend.errors.any? %>
<h2>The following error prevented the todo from being saved
    <h2>
        <ul>
            <% @friend.errors.full_messages.each do |msg| %>
                <li>
                    <%= msg %>
                </li>
                <% end %>
        </ul>
        <% end %>
            <%= form_for @friend do |f| %>
                <p>
                    <%= f.label :name %><br>
                        <%= f.text_field :name%>
                </p>
                <p>
                    <%= f.label :exception %><br>
                        <%= f.text_area :exception %>
                </p>

                <p>
                    <%= f.label :wasChoiced %><br>
                        <%= f.check_box :wasChoiced %>
                </p>

                <p>
                    <%= f.submit %>
                </p>
<% end %>
    <%= link_to  "Back to friends listing", friends_path %>